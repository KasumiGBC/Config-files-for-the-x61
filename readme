Config files for the x61 using Arch and i3. Meant to be cloned on the ~/.config/ directory.

bashmount: only changed file manager

fish: changed colors from the default. Fish_prompt function for customized prompt

i3: changed font, added shortcuts and removed the i3bar

i3/scripts/lock: script for the lock screen for inacivity

i3scripts: custom colors

mpv: only added fixed resolution for streaming

polybar: custom colors, added modules, modified some default modules.

st: patched with scrollback and alpha-focus

thinkfan.yaml: last iteration

omf: colors for the prompt and command line on fish
